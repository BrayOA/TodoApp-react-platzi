import './TodoCounter.css'

function TodoCounter({ total, completed }) {
  return (
    <h1 className='title-h1'>
      Has completado <span>{23}</span> de <span>{23}</span> TODOs
    </h1>
  );
}

export { TodoCounter };
